import test from 'ava';

import axios from 'axios';
import MockAdapter from 'axios-mock-adapter';

test('GET /security/facebook must retrieve or create a user when facebook login succeed', t => {
  t.truthy(true);
});
