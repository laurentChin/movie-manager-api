const formatRouterFactory = require("./router");
const validator = require("./validator");
const typeDef = require("./typeDef");
const resolvers = require("./resolvers");

module.exports = { formatRouterFactory, validator, typeDef, resolvers };
